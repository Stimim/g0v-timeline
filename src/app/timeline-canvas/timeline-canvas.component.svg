<svg [style]="style">
  <rect x="0" y="0" width="100%" height="100%" [attr.fill]="getFillColor()"/>

  <polygon *ngFor="let event of predefinedEvents; index as i"
    [attr.points]="getStarPoints(event, i)" style="fill:yellow" />
</svg>
